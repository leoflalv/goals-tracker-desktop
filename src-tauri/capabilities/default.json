{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "manage"],
  "permissions": [
    "core:default",
    "opener:default",
    "sql:default"
  ]
}
